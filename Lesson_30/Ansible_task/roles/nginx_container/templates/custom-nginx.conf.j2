server {
  listen 80;
  server_name {{ nginx_url }};

  #access_log /var/log/nginx/host.access.log main;
  location / {
    root  /usr/share/nginx/html;
    index index.html index.htm;
   }
  {# location / {
    proxy_pass http://nodejsaca:8080;
  } #}

}